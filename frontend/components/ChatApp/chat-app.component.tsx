'use client';
import { Box } from '@mui/material';

const boxStyles = {
  display: 'flex',
  flexDirection: 'column',
  height: '100vh',
};

export const ChatApp = () => {
  return <Box sx={boxStyles} ></Box>;
};